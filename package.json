{
  "name": "modmyfeed-reddit-prototype",
  "version": "0.9.1",
  "private": true,
  "description": "Dependency-free local Reddit moderation extension prototype.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
